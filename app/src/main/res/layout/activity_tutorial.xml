<?xml version="1.0" encoding="utf-8"?>
<!--
    Tela do tutorial — lista de tópicos indexada por sistema.

    Estrutura:
      - Toolbar com botão de voltar
      - Subtítulo curto explicando o que é a tela
      - RecyclerView vertical com cards de tópico (item_tutorial_topic.xml)
        Tocar num card abre o TutorialDetailDialog com o conteúdo completo.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_screen">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        app:title="@string/tutorial_title"
        app:titleTextColor="@color/color_on_surface"
        android:background="@color/color_surface"
        app:navigationIcon="?attr/homeAsUpIndicator"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:text="@string/tutorial_subtitle"
        android:textColor="@color/color_on_surface_variant"
        android:textSize="13sp"
        android:lineSpacingMultiplier="1.3"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_topics"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingHorizontal="8dp"
        android:paddingBottom="16dp"/>

</LinearLayout>
