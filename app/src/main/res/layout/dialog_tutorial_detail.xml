<?xml version="1.0" encoding="utf-8"?>
<!--
    Dialog de detalhe de um tópico do tutorial.

    Estrutura:
      - Header: emoji grande + título + resumo
      - Lista (ScrollView) com seções: cada uma é título dourado + body em prosa

    O conteúdo das seções vem do TutorialContent.Topic.sections — vai variando
    por tópico, então construímos as views dinamicamente em Kotlin a partir
    desta shell.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/color_surface">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:paddingHorizontal="20dp"
        android:paddingTop="20dp"
        android:paddingBottom="8dp">

        <TextView
            android:id="@+id/tv_detail_emoji"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="56sp"
            tools:text="🎯"/>

        <TextView
            android:id="@+id/tv_detail_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:gravity="center"
            android:textColor="@color/champion_gold"
            android:textSize="20sp"
            android:textStyle="bold"
            android:letterSpacing="0.02"
            tools:text="Conceitos básicos"/>

        <TextView
            android:id="@+id/tv_detail_summary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:gravity="center"
            android:textColor="@color/color_on_surface_variant"
            android:textSize="12sp"
            android:fontFamily="sans-serif-medium"
            tools:text="O que é um split, BO3, divisões e o ciclo de jogo."/>
    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="6dp"
        android:background="@color/color_outline_variant"/>

    <!-- Corpo rolável: seções construídas em Kotlin -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true"
        android:scrollbars="vertical">

        <LinearLayout
            android:id="@+id/ll_sections"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp"/>
    </ScrollView>
</LinearLayout>
