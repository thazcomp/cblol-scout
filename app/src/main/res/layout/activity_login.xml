<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_screen"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:paddingHorizontal="32dp"
        android:paddingVertical="40dp">

        <!-- Logo -->
        <View
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:background="@drawable/bg_login_logo"
            android:layout_marginBottom="20dp"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="CBLOL Scout"
            android:textSize="32sp"
            android:fontFamily="sans-serif-bold"
            android:textColor="@color/color_on_surface"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Gerencie seu time dos sonhos"
            android:textSize="14sp"
            android:textColor="@color/color_on_surface_variant"
            android:layout_marginBottom="36dp"/>

        <!-- Botão único de entrada (sem login/senha) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_guest"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:text="@string/login_start"
            android:textSize="15sp"
            app:cornerRadius="14dp"
            android:backgroundTint="@color/color_primary"/>

        <!--
          Rodapé legal (disclaimer Riot Games).

          É visível em TODA execução do app (não só no onboarding one-shot),
          por conformidade com as diretrizes da Riot para projetos de fã.
          Tocável para abrir o texto completo num diálogo.
        -->
        <TextView
            android:id="@+id/tv_legal_disclaimer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:text="@string/legal_disclaimer_short"
            android:textSize="10sp"
            android:textColor="@color/color_on_surface_variant"
            android:gravity="center"
            android:lineSpacingMultiplier="1.3"
            android:background="?attr/selectableItemBackground"
            android:padding="8dp"/>

    </LinearLayout>
</ScrollView>
